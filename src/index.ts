import { typeValidator, Schema, Validator, baseValidator } from "./validator";
import { valid, invalid } from "./result";
import { typeError, ValidationError, objectError } from "./error";
// tslint:disable:variable-name

export { boolean, number, string, object, def, undef, any } from "./validator";
