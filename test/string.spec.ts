import "mocha";
import { expect } from "chai";
import { string } from "../src";
import { error } from "../src/error";

describe("string", () => {
  describe("accepts", () => {
    it("strings", () => {});
  });
});
